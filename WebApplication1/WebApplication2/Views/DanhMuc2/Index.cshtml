@model IEnumerable<WebApplication2.Models.DanhMuc2>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DanhMuc2Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DanhMuc2Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DanhMuc2ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DanhMuc2ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DanhMuc2ID })
        </td>
    </tr>
}

</table>
