@model IEnumerable<WebApplication4.Models.ConGa2>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ConGa2Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ConGa2Des)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ConGa2Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ConGa2Des)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ConGa2Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.ConGa2Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ConGa2Id })
        </td>
    </tr>
}

</table>
