@model IEnumerable<WebApplication5.Models.DanhMucss>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DanhMucName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DanhMucName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DanhMucId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DanhMucId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DanhMucId })
        </td>
    </tr>
}

</table>
