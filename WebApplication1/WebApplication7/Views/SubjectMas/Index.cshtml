@model IEnumerable<WebApplication7.Models.SubjectMa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SubjectMaName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SubjectMaName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SubjectMaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SubjectMaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SubjectMaId })
        </td>
    </tr>
}

</table>
