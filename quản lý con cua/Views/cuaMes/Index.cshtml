@model IEnumerable<quản_lý_con_cua.Models.cuaMe>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CuaMeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CuaMeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CuaMeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CuaMeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CuaMeId })
        </td>
    </tr>
}

</table>
